#!/usr/bin/make -f

export DH_VERBOSE = 1

%:
	dh $@

override_dh_auto_configure:

override_dh_auto_build:
	bash packaging/obs-build.sh build

override_dh_auto_install:
	bash packaging/obs-build.sh install $(CURDIR)/debian/$(shell dpkg-parsechangelog -S Source)