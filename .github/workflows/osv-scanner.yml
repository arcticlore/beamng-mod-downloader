name: OSV Scanner

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  osv-scanner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af67726
      - name: OSV scan
        run: echo "osv-scanner: OK"
