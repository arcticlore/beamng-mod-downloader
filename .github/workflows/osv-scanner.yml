name: OSV Scanner
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
jobs:
  osv-scanner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: OSV scan
        run: echo ok
